import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const SongsPlayScreen = (props) => {
  return (
    <View>
      <Text>SongsPlayScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default SongsPlayScreen;
